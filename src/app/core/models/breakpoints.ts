// Mirrors $-breakpoints in src/app/styles/_mixins.scss. Only the values that TS
// needs live here; keep both in sync. Breakpoints are structural reflow points,
// so this list stays short by design.
export const BREAKPOINT_LG = 1024;
